[build]
builder = "nixpacks"

[deploy]
startCommand = "uvicorn ogenti_platform.server:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/api/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
